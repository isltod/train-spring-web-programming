<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mycompany.ordersystem.product.repository.ProductRepository">
  <resultMap id="productResult" type="Product">
    <id property="id" column="product_id"/>
    <result property="name" column="product_name"/>
    <result property="description" column="product_description"/>
    <result property="price" column="product_price"/>
  </resultMap>
  <select id="findAll" resultMap="productResult">
    SELECT *
    FROM product
    ORDER BY product_name
  </select>
  <select id="findById" parameterType="long" resultMap="productResult">
    SELECT *
    FROM product
    WHERE product_id = #{id}
  </select>
  <insert id="save" parameterType="Product">
    INSERT INTO product (product_name, product_description, product_price)
    VALUES (#{name}, #{description}, #{price})
  </insert>
  <update id="update" parameterType="Product">
    UPDATE product
    SET product_name        = #{name},
        product_description = #{description},
        product_price       = #{price}
    WHERE product_id = #{id}
  </update>
  <delete id="delete" parameterType="long">
    DELETE
    FROM product
    WHERE product_id = #{id}
  </delete>
</mapper>